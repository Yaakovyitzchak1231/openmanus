# Web browse check

Command used:

```
curl -L -s https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents | head -n 5
```

Result: fetched HTML successfully (starts with `<!DOCTYPE html>`).
