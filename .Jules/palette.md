## 2025-05-23 - Embedded HTML Challenges
**Learning:** This app embeds the entire frontend HTML/CSS/JS in a Python string (`web_ui.py`). This bypasses standard frontend tooling (linting, accessibility checks).
**Action:** Always create a custom Python verification script to parse and validate HTML/JS changes in `web_ui.py` before submitting.
